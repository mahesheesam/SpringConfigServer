After deployment, a scheduler job was implemented to update existing preferences, with an initial delay set for its execution. However, an alert was triggered before the migration script was executed. Subsequent to the scheduler run, the preferences endpoint functioned as expected.
